
{
  "name": "bstech-miniapp",
  "version": "1.0.0",
  "description": "BSTECH Mini App on Base",
  "main": "server.js",
  "scripts": {
    "build": "esbuild index.tsx --bundle --outfile=index.js --format=esm --jsx=automatic --minify --external:react --external:react-dom --external:wagmi --external:viem --external:lucide-react --external:@coinbase/onchainkit --external:@farcaster/miniapp-sdk --external:@google/genai --external:@tanstack/react-query",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "esbuild": "^0.24.0"
  }
}
